# Pacific Basin BI Dashboard - Environment Variables
# Copy to .env and fill in values (do not commit .env to git)

# Optional: AISStream API Key for real vessel positions
# Get free key at: https://aisstream.io/
AISSTREAM_API_KEY=

# Note: Other API keys are not needed as we use server-side proxies
# to hide credentials from browser
